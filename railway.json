{
  "build": {
    "env": {
      "PYTHONUNBUFFERED": "1"
    },
    "builder": "heroku/buildpacks:20",
    "buildCommand": "cd tracker_backend && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd tracker_backend && gunicorn tracker_backend.wsgi"
  }
}
