{
  "build": {
    "env": {
      "PYTHONUNBUFFERED": "1"
    },
    "builder": "heroku/buildpacks:20",
    "buildCommand": "cd backend && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd backend && gunicorn tracker_backend.wsgi"
  }
}
